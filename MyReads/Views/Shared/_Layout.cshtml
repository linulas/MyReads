<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="distribution" content="web">
    <meta property="og:site_name" content="MY Reads" />
    <meta name="description" content="Book app for logging Your Reads, view stats and find new books">
    <meta name="keywords" content="Book,Reads,Stats,Search,Organize">
    <meta name="author" content="Linus Brännström">
    <title>@ViewBag.Title - My Reads</title>
    <!--Custom Fonts-->
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Kaushan+Script" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Droid+Serif:400,700,400italic,700italic" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Roboto+Slab:400,100,300,700" rel="stylesheet" type="text/css">
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body id="page-top" class="index">
    <div class="content">
        <!-- Navigation -->
        <nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
            <div class="container">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header page-scroll">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
                    </button>
                    @Html.ActionLink("My Reads", "Index", "Books", new { userName = User.Identity.Name }, new { @class = "navbar-brand page-scroll" })
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav navbar-right">
                        <li id="navItems">@Html.Partial("_LoginPartial")</li>
                        <li class="page-scroll">
                            <!-- user input -->
                            <div class="userInput">
                                <label for="searchInput" class="hidden">New: </label>
                                <input name="navSearch" id="searchInput" placeholder="Title or Author" value="@ViewBag.Search" autofocus />
                                <!-- user submit -->
                                <button class="icon iconBtn" id="searchBtn" onclick="bookSearch()"><span class="white-custom"><i class="glyphicon glyphicon-search" id="searchIcon"></i></span></button>
                            </div>
                        </li>
                    </ul>
                </div>
                <!-- /.navbar-collapse -->
            </div>
            <!-- /.container-fluid -->
        </nav>
        <main>
            @RenderBody()
            <hr />
            <footer>
                <p>&copy; @DateTime.Now.Year - My Reads</p>
            </footer>
        </main>

    </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.3/jquery.easing.min.js" integrity="sha384-mE6eXfrb8jxl0rzJDBRanYqgBxtJ6Unn4/1F7q4xRRyIw7Vdg9jP4ycT7x1iVsgb" crossorigin="anonymous"></script>
    <!-- Plugin JavaScript -->
    @Scripts.Render("~/bundles/animations")
    @Scripts.Render("~/bundles/googleBooks")
    @RenderSection("scripts", required: false)
</body>
</html>
